matrix:
  platform:
    - debian11
    - macos
    - macos_arm64
    - ubuntu2404
    - windows
  bazel: [ 8.x ]
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '@clay//:clay'