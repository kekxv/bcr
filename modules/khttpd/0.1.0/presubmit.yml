matrix:
  platform:
    - macos_arm64
    - ubuntu2404
  bazel: [8.x]
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - '-@boost.asio//:ssl=boringssl @khttpd//:khttpd'

